<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error - Royal Wedding Planners</title>
    <link href="/css/output.css" rel="stylesheet">
</head>
<body class="bg-gray-100 min-h-screen">
    <div class="container mx-auto px-4 py-8">
        <div class="bg-white rounded-lg shadow-md p-6 max-w-lg mx-auto mt-10">
            <h1 class="text-2xl font-bold text-red-600 mb-4">Error</h1>
            <% if (error && error.message) { %>
                <p class="text-gray-700 mb-4"><%= error.message %></p>
            <% } else { %>
                <p class="text-gray-700 mb-4">An unexpected error occurred. Please try again later.</p>
            <% } %>
            <a href="/" class="inline-block bg-blue-500 text-white px-4 py-2 rounded hover:bg-blue-600">
                Return to Home
            </a>
        </div>
    </div>
</body>
</html>